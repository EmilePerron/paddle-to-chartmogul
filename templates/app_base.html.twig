<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}app{% endblock %} - magicrr</title>
        <link rel="stylesheet" href="{{ asset("css/base.css") }}">
        <link rel="stylesheet" href="{{ asset("css/app.css") }}">

		<link rel="apple-touch-icon" sizes="152x152" href="{{ asset("apple-touch-icon.png") }}">
		<link rel="icon" type="image/png" sizes="32x32" href="{{ asset("favicon-32x32.png") }}">
		<link rel="icon" type="image/png" sizes="16x16" href="{{ asset("favicon-16x16.png") }}">
		<link rel="manifest" href="{{ asset("site.webmanifest") }}">
		<link rel="mask-icon" href="{{ asset("safari-pinned-tab.svg") }}" color="#13204d">
		<meta name="msapplication-TileColor" content="#da532c">
		<meta name="theme-color" content="#13204d">

		<meta name="description" content="Sync your Paddle subscriptions to ChartMogul automagically.">
		<meta property="og:title" content="Paddle to ChartMogul integration - magicrr">
		<meta property="og:image" content="{{ absolute_url(asset("images/magicrr-social-share.png")) }}">
		<meta name="twitter:card" content="summary_large_image">
		<meta name="viewport" content="width=device-width, initial-scale=1">

		<meta name="robots" content="{% block robots %}noindex,follow{% endblock %}">

        {% block stylesheets %}

        {% endblock %}
    </head>
    <body>
		<div class="container">
			<div id="logo-wrapper">
				<a href="/">
					<img src="{{ asset("images/logo.png") }}" alt="magicrr" width="275" height="100">
				</a>
			</div>

			<div id="app">
				<nav>
					<a href="{{ path("dashboard") }}" aria-current="{{ "dashboard" == app.request.get('_route') ? "true" : "false" }}">Dashboard</a>
					<a href="{{ path("settings") }}" aria-current="{{ "settings" == app.request.get('_route') ? "true" : "false" }}">Settings</a>
					<a href="{{ path("app_logout") }}">Log out</a>
				</nav>
				<div class="card">
					{% for type, messages in app.flashes %}
						{% for message in messages %}
							<div class="notice {{ type }}">
								{{ message }}
							</div>
						{% endfor %}
					{% endfor %}

					{% block body %}{% endblock %}
				</div>
			</div>

			<footer>
				<div class="container">
					<p>
						magicrr is brought to you by <a href="https://www.koalati.com" target="_blank">Koalati</a>.<br>
						You can provide feedback to the creator <a href="https://twitter.com/intent/tweet?screen_name=cunrakes&text=Hey!+I+checked+out+magicrr.koalati.com+and..." target="_blank">on Twitter</a>.
					</p>
				</div>
			</footer>
		</div>

        {% block javascripts %}

        {% endblock %}
    </body>
</html>
